import styled from "styled-components"

export const TitleOcupation = styled.h4`
    font-size: 1.375em;
    color: ${({ theme }) => theme.COLORS.WHITE};
    font-weight: 500;
    margin-bottom: 15px;
    margin-top: 30px;
`
export const TimeOcupation = styled.p`
    font-size: 1.125em;
    color: ${({ theme }) => theme.COLORS.GRAY};
    font-weight: 500;
`